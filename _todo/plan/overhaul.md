
# Overhaul

I still want to avoid going down the route of building a really detailed website for this.

BUT

I want this website to be nice and robust.

## Makefile

The Makefile should be my orchestrator. It should be well documented and live on the top level of the repo.

## Data Management

We need a suite of data management tools. We kinda have this with scripts, but I think they can be improved. 
Some should run automatically, others should be called more easily from the Makefile.

## Theme

We should just have a better css theme that applies to all pages and the Chrome extension.

## Markdown

Some of the site is hard to edit, markdown might be a good way around this.
I don't want to make this a whole Jekyll app, I think that's overkill as there is no blog.

## Flashcard page

Should not have to update manually with the sets, should use the sets manifest file.

## Local Storage

I should be able to update my local storage from the Chrome Extension the same way the website does.
